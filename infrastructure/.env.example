# Copy this file to infrastructure/.env and fill in a real password.
# infrastructure/.env itself is gitignored — never commit real credentials.

IDENTITY_DB_NAME=identity_db
IDENTITY_DB_USERNAME=identity_service
IDENTITY_DB_PASSWORD=changeme
IDENTITY_DB_PORT=5432
